---
// /dashboard is retired in favour of /campaigns. Keep the route alive so old
// bookmarks/links don't 404, but redirect to the new home. Use 302 (not 301):
// a permanent redirect is cached indefinitely by browsers and would strand
// users if /dashboard ever becomes a real page again.
return Astro.redirect("/campaigns", 302);
---
